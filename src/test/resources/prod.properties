# Production environment configuration (usually for smoke tests only)
base.url=https://api.example.com
timeout.request=60
timeout.response=60
retry.count=1
parallel.threads=2

# Prod specific auth
auth.basic.username=prod_user
auth.basic.password=prod_pass